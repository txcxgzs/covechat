# Docker Compose deployment reverse proxy. Change the domain and keep the
# upstream bound to loopback; Caddy provides HTTPS and WebSocket forwarding.
chat.example.com {
    encode zstd gzip
    reverse_proxy 127.0.0.1:8088
}
